export const TRUCK_FEES = [
  { city: "평택시", fee1t: 50000, fee5t: 90000 },
  { city: "오산시", fee1t: 50000, fee5t: 90000 },
  { city: "안성시", fee1t: 64000, fee5t: 110000 },
  { city: "화성시", fee1t: 62000, fee5t: 110000 },
  { city: "수원시", fee1t: 57000, fee5t: 110000 },
  { city: "용인시", fee1t: 58000, fee5t: 110000 },
  { city: "안산시", fee1t: 73000, fee5t: 130000 },
  { city: "시흥시", fee1t: 78000, fee5t: 140000 },
  { city: "성남시", fee1t: 76000, fee5t: 130000 },
  { city: "광명시", fee1t: 86000, fee5t: 150000 },
  { city: "부천시", fee1t: 90000, fee5t: 150000 },
  { city: "과천시", fee1t: 79000, fee5t: 140000 },
  { city: "군포시", fee1t: 72000, fee5t: 130000 },
  { city: "의왕시", fee1t: 65000, fee5t: 120000 },
  { city: "안양시", fee1t: 71000, fee5t: 130000 },
  { city: "김포시", fee1t: 103000, fee5t: 170000 },
  { city: "고양시", fee1t: 104000, fee5t: 170000 },
  { city: "파주시", fee1t: 114000, fee5t: 190000 },
  { city: "이천시", fee1t: 83000, fee5t: 140000 },
  { city: "여주시", fee1t: 98000, fee5t: 170000 },
  { city: "양평군", fee1t: 101000, fee5t: 170000 },
  { city: "하남시", fee1t: 83000, fee5t: 140000 },
  { city: "광주시(경기)", fee1t: 79000, fee5t: 140000 },
  { city: "포천시", fee1t: 128000, fee5t: 210000 },
  { city: "연천군", fee1t: 152000, fee5t: 240000 },
  { city: "가평군", fee1t: 129000, fee5t: 210000 },
  { city: "서울시", fee1t: 93000, fee5t: 160000 },
  { city: "인천시", fee1t: 91000, fee5t: 160000 },
  { city: "천안시", fee1t: 73000, fee5t: 130000 },
  { city: "아산시", fee1t: 75000, fee5t: 130000 },
  { city: "당진시", fee1t: 84000, fee5t: 140000 },
  { city: "서산시", fee1t: 102000, fee5t: 170000 },
  { city: "태안군", fee1t: 118000, fee5t: 190000 },
  { city: "예산군", fee1t: 88000, fee5t: 150000 },
  { city: "홍성군", fee1t: 102000, fee5t: 170000 },
  { city: "보령시", fee1t: 131000, fee5t: 210000 },
  { city: "청주시", fee1t: 99000, fee5t: 170000 },
  { city: "세종시", fee1t: 107000, fee5t: 180000 },
  { city: "대전시", fee1t: 120000, fee5t: 200000 },
  { city: "논산시", fee1t: 132000, fee5t: 210000 },
  { city: "계룡시", fee1t: 130000, fee5t: 210000 },
  { city: "공주시", fee1t: 108000, fee5t: 180000 },
  { city: "부여군", fee1t: 130000, fee5t: 210000 },
  { city: "금산군", fee1t: 150000, fee5t: 240000 },
  { city: "제천시", fee1t: 131000, fee5t: 210000 },
  { city: "충주시", fee1t: 112000, fee5t: 180000 },
  { city: "단양군", fee1t: 145000, fee5t: 230000 },
  { city: "전주시", fee1t: 166000, fee5t: 260000 },
  { city: "익산시", fee1t: 155000, fee5t: 250000 },
  { city: "군산시", fee1t: 156000, fee5t: 250000 },
  { city: "김제시", fee1t: 169000, fee5t: 270000 },
  { city: "정읍시", fee1t: 191000, fee5t: 290000 },
  { city: "남원시", fee1t: 206000, fee5t: 310000 },
  { city: "광주광역시", fee1t: 229000, fee5t: 340000 },
  { city: "나주시", fee1t: 243000, fee5t: 360000 },
  { city: "목포시", fee1t: 267000, fee5t: 390000 },
  { city: "해남군", fee1t: 290000, fee5t: 430000 },
  { city: "완도군", fee1t: 318000, fee5t: 470000 },
  { city: "여수시", fee1t: 270000, fee5t: 400000 },
  { city: "순천시", fee1t: 250000, fee5t: 370000 },
  { city: "광양시", fee1t: 254000, fee5t: 370000 },
  { city: "대구광역시", fee1t: 210000, fee5t: 320000 },
  { city: "구미시", fee1t: 179000, fee5t: 270000 },
  { city: "김천시", fee1t: 166000, fee5t: 260000 },
  { city: "안동시", fee1t: 181000, fee5t: 280000 },
  { city: "영주시", fee1t: 172000, fee5t: 260000 },
  { city: "문경시", fee1t: 153000, fee5t: 240000 },
  { city: "포항시", fee1t: 245000, fee5t: 360000 },
  { city: "경주시", fee1t: 246000, fee5t: 360000 },
  { city: "울산광역시", fee1t: 270000, fee5t: 400000 },
  { city: "부산광역시", fee1t: 282000, fee5t: 410000 },
  { city: "창원시", fee1t: 260000, fee5t: 380000 },
  { city: "김해시", fee1t: 269000, fee5t: 400000 },
  { city: "진주시", fee1t: 242000, fee5t: 360000 },
  { city: "사천시", fee1t: 257000, fee5t: 380000 },
  { city: "통영시", fee1t: 281000, fee5t: 410000 },
  { city: "거제시", fee1t: 285000, fee5t: 420000 },
];

// ===== 적재 알고리즘 (v2 — 실전 적재 규칙 기반) =====

interface CartItemForTruck {
  category?: "flashing" | "swing" | "hanga" | "cleanroom-al" | "door-al" | "panel" | "gutter" | "accessory" | "hardware";
  productName: string;
  size: string;
  qty: number;
}

// 행가도어 사이즈에서 가로세로(mm) 추출
function parseHangaSize(size: string): { w: number; h: number } {
  const match = size.match(/(\d+)×(\d+)/);
  if (!match) return { w: 0, h: 0 };
  return { w: parseInt(match[1]), h: parseInt(match[2]) };
}

// 편개 환산 수량 계산
function toPanels(item: CartItemForTruck): number {
  const isDouble = item.productName.includes("양개");
  return isDouble ? item.qty * 2 : item.qty;
}

export interface TruckResult {
  type: "self" | "parcel" | "1t" | "5t" | "combo" | "inquiry";
  label: string;
  desc: string;
  fee1t: number;
  fee5t: number;
  trucks: { size: "1t" | "5t"; count: number }[];
  totalFee: number;
  warning?: string;
}

// 행가도어 최대 한짝폭/짧은변 계산
function getHangaMaxDimensions(items: CartItemForTruck[]): { maxLeafW: number; maxMinSide: number } {
  let maxLeafW = 0;
  let maxMinSide = 0;
  for (const item of items.filter(i => i.category === "hanga")) {
    const { w, h } = parseHangaSize(item.size);
    const isDouble = item.productName.includes("양개");
    const leafW = isDouble ? Math.ceil(w / 2) : w;
    const minSide = Math.min(leafW, h);
    if (leafW > maxLeafW) maxLeafW = leafW;
    if (minSide > maxMinSide) maxMinSide = minSide;
  }
  return { maxLeafW, maxMinSide };
}

// ── 1톤 행가 적재 한도 (한짝폭 기준) ──
function hanga1tLimit(maxLeafW: number): number {
  if (maxLeafW <= 1500) return 4;
  if (maxLeafW <= 2000) return 2;
  if (maxLeafW <= 2500) return 1;
  return 0; // 2500 초과 → 1톤 불가
}

// ── 5톤 행가 적재 한도 (2단×6 = 12짝) ──
const HANGA_5T_LIMIT = 12;

export function calcTruckOptions(items: CartItemForTruck[], regionCity: string): TruckResult[] {
  const region = TRUCK_FEES.find(r => r.city === regionCity);
  const fee1t = region?.fee1t ?? 0;
  const fee5t = region?.fee5t ?? 0;

  const flashingQty = items.filter(i => i.category === "flashing").reduce((s, i) => s + i.qty, 0);
  const swingPanels = items.filter(i => i.category === "swing").reduce((s, i) => s + toPanels(i), 0);
  const hangaPanels = items.filter(i => i.category === "hanga").reduce((s, i) => s + toPanels(i), 0);
  const alQty = items.filter(i => i.category === "cleanroom-al" || i.category === "door-al").reduce((s, i) => s + i.qty, 0);
  const panelQty = items.filter(i => i.category === "panel").reduce((s, i) => s + i.qty, 0);
  // 판넬 최대 길이 확인 (4000/5000/6000mm는 1톤 불가)
  const panelMaxLen = items.filter(i => i.category === "panel").reduce((max, i) => {
    const m = i.size.match(/×\s*(\d+)mm/);
    return m ? Math.max(max, parseInt(m[1])) : max;
  }, 0);
  const hasLongPanel = panelMaxLen > 3000; // 1톤 적재 불가 판넬
  const accQty = items.filter(i => i.category === "gutter" || i.category === "accessory" || i.category === "hardware").reduce((s, i) => s + i.qty, 0);
  const hasHanga = hangaPanels > 0;
  const hasSwing = swingPanels > 0;
  const hasFlashing = flashingQty > 0;
  const hasAl = alQty > 0;
  const hasPanel = panelQty > 0;
  const hasAcc = accQty > 0;

  const { maxLeafW, maxMinSide } = getHangaMaxDimensions(items);

  const results: TruckResult[] = [];

  // ━━━ 짧은변 > 3000mm → 용차 자체 불가 ━━━
  if (hasHanga && maxMinSide > 3000) {
    results.push({
      type: "inquiry", label: "⚠️ 가조립/현장제작",
      desc: "짧은변이 3000mm를 초과하여 완조립 용차 배송이 불가합니다. 가조립 또는 현장제작으로 진행해 주세요.",
      fee1t, fee5t, trucks: [], totalFee: 0,
      warning: "짧은변 3000mm 초과 — 가조립/현장제작 필요",
    });
    return results;
  }

  // ━━━ 1톤 적재 판정 ━━━
  const h1Limit = hasHanga ? hanga1tLimit(maxLeafW) : 0;
  const desc1 = (parts: string[]) => {
    const extras = [
      hasAl ? `AL ${alQty}본` : "",
      hasPanel ? `판넬 ${panelQty}장` : "",
      hasAcc ? `부자재 ${accQty}개` : "",
    ];
    return [...parts, ...extras].filter(Boolean).join(" + ");
  };

  // 4000mm 이상 판넬이 있으면 1톤 불가 (적재함 길이 3000mm)
  if (!hasLongPanel) {

  if (hasHanga) {
    // 행가 + (스윙/후레싱)
    if (h1Limit > 0 && hangaPanels <= h1Limit) {
      // 행가가 절반 이하면 여유, 절반 초과면 빡빡
      const halfFull = hangaPanels > h1Limit * 0.5;
      const swingRemain = halfFull ? 3 : 4;
      // 후레싱은 작아서 아래에 깔 수 있으므로 항상 100개까지
      const flashRemain = 100;

      if (swingPanels <= swingRemain && flashingQty <= flashRemain) {
        results.push({
          type: "1t", label: "🚛 1톤 용차",
          desc: desc1([`행가 ${hangaPanels}짝`, hasSwing ? `스윙 ${swingPanels}조` : "", hasFlashing ? `후레싱 ${flashingQty}개` : ""]),
          fee1t, fee5t, trucks: [{ size: "1t", count: 1 }], totalFee: fee1t,
        });
      }
    }
  } else if (hasSwing) {
    // 스윙 only (+ 후레싱: 스윙 여유분에 비례)
    const swingUsage = swingPanels / 20; // 0~1
    const flashRemain = swingUsage <= 0.5 ? 150 : 40;
    if (swingPanels <= 20 && flashingQty <= flashRemain) {
      results.push({
        type: "1t", label: "🚛 1톤 용차",
        desc: desc1([`스윙 ${swingPanels}조`, hasFlashing ? `후레싱 ${flashingQty}개` : ""]),
        fee1t, fee5t, trucks: [{ size: "1t", count: 1 }], totalFee: fee1t,
      });
    }
  } else if (hasFlashing) {
    // 후레싱 only
    if (flashingQty <= 300) {
      results.push({
        type: "1t", label: "🚛 1톤 용차",
        desc: desc1([`후레싱 ${flashingQty}개`]),
        fee1t, fee5t, trucks: [{ size: "1t", count: 1 }], totalFee: fee1t,
      });
    }
  } else if (hasAl || hasPanel || hasAcc) {
    // AL/판넬/부자재 only
    results.push({
      type: "1t", label: "🚛 1톤 용차",
      desc: desc1([]),
      fee1t, fee5t, trucks: [{ size: "1t", count: 1 }], totalFee: fee1t,
    });
  }
  } // end !hasLongPanel

  // ━━━ 5톤 적재 판정 ━━━
  if (hasHanga) {
    if (hangaPanels <= HANGA_5T_LIMIT) {
      // 5톤 1대: 앞열 행가, 뒷열 스윙/후레싱
      const frontUsed = hangaPanels > 6; // 앞열 넘어서 뒷열까지 행가
      const swingRemain5 = frontUsed ? 0 : 32;
      const flashRemain5 = frontUsed ? 0 : 300;

      if (swingPanels <= swingRemain5 && flashingQty <= flashRemain5) {
        results.push({
          type: "5t", label: "🚛 5톤 용차",
          desc: desc1([`행가 ${hangaPanels}짝`, hasSwing ? `스윙 ${swingPanels}조` : "", hasFlashing ? `후레싱 ${flashingQty}개` : ""]),
          fee1t, fee5t, trucks: [{ size: "5t", count: 1 }], totalFee: fee5t,
        });
      } else {
        // 5톤 1대로 부족 → 5톤 + 1톤 조합
        const extra1t = 1;
        results.push({
          type: "combo", label: "🚛 5톤×1 + 1톤×1",
          desc: "행가는 5톤, 나머지는 1톤 분리 배송",
          fee1t, fee5t, trucks: [{ size: "5t", count: 1 }, { size: "1t", count: extra1t }],
          totalFee: fee5t + fee1t * extra1t,
        });
      }
    } else {
      // 행가만으로 5톤 초과 → 복수 5톤
      const need5t = Math.ceil(hangaPanels / HANGA_5T_LIMIT);
      const needExtra = (swingPanels > 0 || flashingQty > 0) ? 1 : 0;
      results.push({
        type: "combo", label: `🚛 5톤×${need5t}${needExtra ? " + 1톤×1" : ""}`,
        desc: "적재량 초과로 복수 차량 필요",
        fee1t, fee5t, trucks: [{ size: "5t", count: need5t }, ...(needExtra ? [{ size: "1t" as const, count: 1 }] : [])],
        totalFee: fee5t * need5t + fee1t * needExtra,
      });
    }
  } else if (hasSwing) {
    // 스윙 (+ 후레싱): 앞열 스윙, 뒷열 후레싱
    const swingLimit5 = flashingQty > 0 ? 32 : 64;
    const flashLimit5 = swingPanels > 32 ? 0 : 300;

    if (swingPanels <= swingLimit5 && flashingQty <= flashLimit5) {
      results.push({
        type: "5t", label: "🚛 5톤 용차",
        desc: desc1([`스윙 ${swingPanels}조`, hasFlashing ? `후레싱 ${flashingQty}개` : ""]),
        fee1t, fee5t, trucks: [{ size: "5t", count: 1 }], totalFee: fee5t,
      });
    } else {
      const need5t = Math.ceil(swingPanels / 64);
      const needFlash1t = flashingQty > flashLimit5 ? Math.ceil(flashingQty / 300) : 0;
      results.push({
        type: "combo", label: `🚛 5톤×${need5t}${needFlash1t ? ` + 1톤×${needFlash1t}` : ""}`,
        desc: "적재량 초과로 복수 차량 필요",
        fee1t, fee5t, trucks: [{ size: "5t", count: need5t }, ...(needFlash1t ? [{ size: "1t" as const, count: needFlash1t }] : [])],
        totalFee: fee5t * need5t + fee1t * needFlash1t,
      });
    }
  } else if (hasFlashing) {
    // 후레싱 only
    if (flashingQty <= 1000) {
      results.push({
        type: "5t", label: "🚛 5톤 용차",
        desc: desc1([`후레싱 ${flashingQty}개`]),
        fee1t, fee5t, trucks: [{ size: "5t", count: 1 }], totalFee: fee5t,
      });
    } else {
      const need5t = Math.ceil(flashingQty / 1000);
      results.push({
        type: "combo", label: `🚛 5톤×${need5t}`,
        desc: "적재량 초과로 복수 차량 필요",
        fee1t, fee5t, trucks: [{ size: "5t", count: need5t }],
        totalFee: fee5t * need5t,
      });
    }
  } else if (hasAl || hasPanel || hasAcc) {
    // AL/판넬/부자재 only
    results.push({
      type: "5t", label: "🚛 5톤 용차",
      desc: desc1([]),
      fee1t, fee5t, trucks: [{ size: "5t", count: 1 }], totalFee: fee5t,
    });
  }

  return results;
}
